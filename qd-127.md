# qd-127 - 商业步行街公共休息长凳街区分段树形分类管理系统

## 项目简介

本系统为商业步行街运营后勤管理户外休息长凳的专用管理系统，采用街区-路段-点位三级树形分类架构，支持自主维护整套分类体系。长凳档案录入后绑定三级树形点位；街区业态调整可修改单条长凳所属路段点位，留存分类变更记录；支持新增、编辑、删除任意层级树形分类节点；树形目录可逐层展开筛选对应点位全部长凳，支持导出单一路段完整资产目录。

### 核心功能模块

1. **户外长凳基础建档** - 编号、材质、尺寸（长度/宽度/高度）、摆放点位属性
2. **三级树形点位绑定** - 分配长凳所属街区、路段、点位，联动选择
3. **点位归属变更登记** - 调整长凳树形分类，留存变更台账（旧节点、新节点、变更原因）
4. **树形分类节点管理** - 新增、编辑、删除任意层级树形分类节点，层级校验
5. **资产筛选与导出** - 逐层展开树形目录筛选点位资产清单，支持导出单一路段完整资产目录
6. **长凳巡检与维修闭环** - 按街区/路段/点位发起巡检（检查时间、问题类型、严重程度、问题描述、处理建议），异常长凳可生成维修工单；工单支持待处理→维修中→已完成→已关闭流转，完成时强制填写维修结果和完成时间；资产列表/详情展示最新巡检状态，支持按巡检状态与严重程度筛选；仍有未完成工单的长凳禁止删除
7. **巡检计划与任务派发** - 按街区/路段/点位配置周期（每天/每周/每月）、计划日期、检查人和启用状态，系统自动生成待执行任务并标记逾期；执行任务时自动带入范围内长凳，提交后关联巡检记录；计划详情展示已执行、待执行、逾期统计；停用计划不再生成新任务，重复执行与空范围给出明确提示
8. **点位夜间照明巡查** - 按点位登记夜间照明是否完好、灯具数量和巡查人（点位必选，未选点位不能提交）；异常时必须点名缺灯或损坏；街区树点位展示最近一次照明结论（完好/异常/未巡查标记）；未巡查点位可单独筛出；按路段汇总异常点数、缺灯与损坏各多少（没有异常的路段不出汇总），街区树路段展示异常点数、点开可下钻到点位明细；树标记、路段汇总、点位列表与巡查记录同源，刷新后保持一致
9. **路段容量告警** - 路段下各点位剩余容量（按在用长凳计）加总低于阈值（默认10）、且至少存在一个已满（剩余0）或将满（剩余≤2）点位时，街区树路段节点标记容量告警并显示剩余加总；点开告警可下钻查看该路段已满或将满的点位明细（保证有告警就一定能对上明细点位，没有已满/将满点位的路段不出告警）；树页面头部展示容量告警条数；树上标记、下钻点位列表与告警条数同源，刷新后保持一致；没有点位的路段不出告警
10. **节假日加凳预案** - 按街区选择投放路段，登记计划日期、加凳数量并提交待投放预案；未选路段或数量为0不能提交；提交后街区树对应路段显示待投放数量，计划日期过期且未投放完自动标记逾期；已投放数量支持分批核销，每次核销留存数量、操作人、时间和备注，全部核销后预案变为已投放
11. **商户长凳冠名台账** - 按点位（街区→路段→点位三级联动）提交商户名称、冠名文案和冠名起止日期；未选点位或商户名称/文案为空、结束日期早于开始日期不能提交；提交后台账出现待生效记录并持久化到数据库，关掉页面再打开仍能看到；到达开始日期自动变为生效中，结束日前3天自动标记即将到期并展示到期日与剩余天数，结束日期过后自动标记已过期（状态按当天日期动态推导，不落库）；冠名台账和街区树点位、路段均展示临期提醒，支持按即将到期筛选，没有记录时列表明确提示为空

### 技术架构

- **前端**：Vue3 + Vite + Element Plus
- **后端**：Spring Boot 3.3 + JDK 17 + JPA
- **数据库**：MySQL 8.0
- **缓存**：Redis 有序集合缓存长凳尺寸规格参数
- **容器**：Docker + Docker Compose

## 访问地址

- **前端页面**：http://localhost:8227
- **后端API**：http://localhost:8327/api
- **MySQL**：localhost:3527
- **Redis**：localhost:6627

## 启动方式

```bash
cd qd-127
bash start.sh
```

或直接使用 Docker Compose：

```bash
docker compose up -d --build
```

## 项目特点

- 三级树形分类架构（街区→路段→点位）
- 树形节点增删改及层级校验
- 长凳分类变更日志独立存储
- Redis有序集合缓存长凳规格参数
- 支持批量分类调整
- 支持资产目录导出
- 区分开发/生产两套配置
- 国内镜像源加速（阿里云Maven、淘宝npm）
